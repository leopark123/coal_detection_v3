# 上下文衔接表（每次切换 AI 必填）

## 使用规则

1. 新 AI 开始前，先读最新一条记录。
2. 新 AI 结束前，必须新增一条记录，不覆盖旧记录。
3. 每条记录必须包含证据路径和下一步可执行入口。

## 固定字段模板

| 衔接ID | 日期 | 当前目标 | 本轮完成 | 产出文件 | 关键决策 | 风险/阻塞 | 证据路径 | 下一步 | 执行入口 |
|---|---|---|---|---|---|---|---|---|---|
| H-YYYYMMDD-001 | YYYY-MM-DD | 本轮目标一句话 | 已完成事项列表 | 文件路径列表 | 本轮确认的决策 | 未解决问题 | 日志或代码或表格路径 | 下一步动作一句话 | 命令或文件入口 |

## 最新衔接记录

| 衔接ID | 日期 | 当前目标 | 本轮完成 | 产出文件 | 关键决策 | 风险/阻塞 | 证据路径 | 下一步 | 执行入口 |
|---|---|---|---|---|---|---|---|---|---|
| H-20260219-001 | 2026-02-19 | 在模拟阶段完成稳定化并建立交接体系 | 完成Web公共循环收敛、静态挂载统一、异常处理收敛、新增公共模块测试并全量回归通过 | `web/common.py`; `web/app.py`; `core/capture_process.py`; `core/detect_process.py`; `tests/test_web_common.py`; `handoff/*` | 先模拟后生产，生产链路等待测试门禁 | 模拟样本覆盖仍需扩充；真实设备链路尚未接入 | `python -m pytest -q` => `63 passed, 1 skipped`；`python -m pytest -q tests/test_web_common.py` => `8 passed` | 扩充模拟样本与场景覆盖，并准备生产链路接入设计 | `handoff/开发进度表.csv` |
| H-20260219-002 | 2026-02-19 | 将WP-004细化为可执行子任务并固化接口契约回归 | 新增`tests/test_web_routes_contract.py`并通过；补全`接口契约表`全部Web入口；将WP-004拆分为A/B/C/D子任务并回填进度 | `tests/test_web_routes_contract.py`; `handoff/开发进度表.csv`; `handoff/接口契约表.csv`; `handoff/测试与验收表.csv`; `handoff/决策记录表_ADR.csv`; `handoff/变更日志表.csv` | 接口清单必须配自动校验，避免仅文档约束 | 模拟样本覆盖仍不足；长稳手测尚未执行 | `python -m pytest -q tests/test_web_routes_contract.py` => `3 passed`；`python -m pytest -q` => `66 passed, 1 skipped` | 继续推进WP-004C（样本扩充）并执行WP-004D（30分钟长稳手测） | `handoff/开发进度表.csv` |
| H-20260219-003 | 2026-02-19 | 推进WP-004C并形成可执行补样基线 | 完成模拟样本盘点并输出分类缺口；在进度表新增WP-004C1；同步更新数据集与验收表、ADR、变更日志 | `handoff/模拟样本盘点.md`; `handoff/开发进度表.csv`; `handoff/数据集与标注表.csv`; `handoff/测试与验收表.csv`; `handoff/决策记录表_ADR.csv`; `handoff/变更日志表.csv` | 补样前先盘点，避免样本扩充失衡 | 重煤/边界/光照样本仍不足；长稳手测未执行 | `tests/mock_data`=9, `tests/test_images`=2, `tests/sample_only`=1；`handoff/模拟样本盘点.md` | 先补三类缺口样本并映射到集成测试，再执行30分钟长稳手测 | `handoff/开发进度表.csv` |

## 下一任 AI 启动提示词（可直接复用）

```text
请先读取 handoff/上下文衔接表.md 最后一条记录，
再读取 handoff/开发计划.md、handoff/开发进度表.csv、handoff/开发约束表.csv。
按进行中任务继续推进，完成后回写进度表、测试表、ADR和上下文衔接表。
```
